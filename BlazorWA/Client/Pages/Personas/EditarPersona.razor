@page "/personas/editar/{PersonaId:int}"

<h3>Editar Persona</h3>

<FormularioPersonas Persona="Persona" OnValidSubmit="Editar"></FormularioPersonas>

@code {
    [Parameter] public int PersonaId { get; set; }
    Persona Persona = new Persona();

    protected override void OnInitialized()
    {
        base.OnInitialized();
        Persona = new Persona() { Id=PersonaId, Nombre="Ervin", FechaNacimiento = DateTime.Today};
    }

    private void Editar() {
        Console.WriteLine("Editando la persona");
    }
}
